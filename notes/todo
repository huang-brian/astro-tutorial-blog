fix the padding on the homelayout not consistent with other pages 404 and extendedbase
it shoule only be
      padding-bottom: 0.5rem;

make the nav bar two columns for when I have extra stuff


reduce all image sizes in the project to 50% of their original size and set the quality to 85% to optimize loading times. Use the following command in the terminal while in the directory containing the images
magick mogrify -resize 50% -quality 85 -auto-orient -strip *.jpeg   
